# Source this file in ~/.bashrc or ~/.zshrc to get the `redeploy` command.
# Update the path below to match where kalpataru-grove is cloned on this machine.
redeploy() {
  /path/to/kalpataru-grove/redeploy_script.sh "$@"
}
